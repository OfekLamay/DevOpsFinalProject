# Generated by Selenium IDE
import pytest
import time
import json
from selenium import webdriver
from selenium.webdriver.common.by import By
from selenium.webdriver.common.action_chains import ActionChains
from selenium.webdriver.support import expected_conditions
from selenium.webdriver.support.wait import WebDriverWait
from selenium.webdriver.common.keys import Keys
from selenium.webdriver.common.desired_capabilities import DesiredCapabilities

class TestWholeusagetest():
  def setup_method(self, method):
    self.driver = webdriver.Firefox()
    self.vars = {}
  
  def teardown_method(self, method):
    self.driver.quit()
  
  def wait_for_window(self, timeout = 2):
    time.sleep(round(timeout / 1000))
    wh_now = self.driver.window_handles
    wh_then = self.vars["window_handles"]
    if len(wh_now) > len(wh_then):
      return set(wh_now).difference(set(wh_then)).pop()
  
  def test_wholeusagetest(self):
    self.driver.get("http://165.245.208.238:8080/OfekLamay_BenLetai_LinoyAvraham_TomerBiton_AmitHazan/index.jsp")
    assert self.driver.title == "DevOps Final Project"
    elements = self.driver.find_elements(By.CSS_SELECTOR, ".btn")
    assert len(elements) > 0
    self.driver.find_element(By.NAME, "q").click()
    elements = self.driver.find_elements(By.ID, "error-message")
    assert len(elements) == 0
    elements = self.driver.find_elements(By.CSS_SELECTOR, "a[href*=\'github.com\']")
    assert len(elements) > 0
    self.vars["window_handles"] = self.driver.window_handles
    self.driver.find_element(By.XPATH, "//input[@value=\'חפש בגוגל\']").click()
    self.vars["win9999"] = self.wait_for_window(2000)
  
